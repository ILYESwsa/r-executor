<Window x:Class="DiscordOAuthWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:local="clr-namespace:DiscordOAuthWpf"
        mc:Ignorable="d"
        Title="Mega Windows Performance Tweaks Utility"
        Height="760"
        Width="1220"
        MinHeight="680"
        MinWidth="980"
        WindowStartupLocation="CenterScreen"
        Background="#060B17"
        Foreground="#E2E8F0">
  <Window.Resources>
    <LinearGradientBrush x:Key="AppBackground" StartPoint="0,0" EndPoint="1,1">
      <GradientStop Color="#0A1328" Offset="0"/>
      <GradientStop Color="#060B17" Offset="1"/>
    </LinearGradientBrush>

    <Style x:Key="PrimaryButton" TargetType="Button">
      <Setter Property="Background" Value="#3366FF"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="16,9"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" CornerRadius="10">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#4D77FF"/>
        </Trigger>
      </Style.Triggers>
    </Style>

    <Style x:Key="FlatPanel" TargetType="Border">
      <Setter Property="CornerRadius" Value="12"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Background" Value="#111B31"/>
    </Style>
  </Window.Resources>

  <Grid Margin="16">
    <Border CornerRadius="14" BorderBrush="#314169" BorderThickness="1" Background="{StaticResource AppBackground}">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="66"/>
          <RowDefinition Height="*"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#1A2542">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>

          <TextBlock Grid.Column="0"
                     Text="MEGA WINDOWS PERFORMANCE TWEAKS"
                     Margin="20,0,0,0"
                     VerticalAlignment="Center"
                     FontSize="28"
                     FontWeight="Bold"
                     Foreground="#BBD0FF"/>

          <Border Grid.Column="1" Margin="0,14,18,14" Padding="16,6" Background="#101A2F" CornerRadius="20">
            <TextBlock x:Name="TopUserBadge" Text="Not authenticated" VerticalAlignment="Center" Foreground="#D7E3FF" FontWeight="SemiBold"/>
          </Border>
        </Grid>

        <Grid Grid.Row="1" Margin="14">
          <Border x:Name="WebViewContainer" Style="{StaticResource FlatPanel}">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>

              <DockPanel Margin="14,12,14,10">
                <Button x:Name="LoginButton"
                        Style="{StaticResource PrimaryButton}"
                        Content="Login with Discord"
                        Width="190"
                        Height="38"
                        Click="LoginButton_Click"
                        Margin="0,0,12,0" />
                <TextBlock x:Name="UserText" VerticalAlignment="Center" Foreground="#A5B4FC" FontWeight="SemiBold" Text="Not authenticated"/>
              </DockPanel>

              <Border Grid.Row="1" Margin="14,0,14,14" CornerRadius="10" Background="#0D1528">
                <wv2:WebView2 x:Name="OAuthWebView" />
              </Border>
            </Grid>
          </Border>

          <Grid x:Name="NativeContentContainer" Visibility="Collapsed">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource FlatPanel}" Padding="18" Margin="0,0,0,12">
              <DockPanel>
                <StackPanel>
                  <TextBlock x:Name="UsernameValue" Text="Username: -" FontSize="18" FontWeight="SemiBold" Foreground="#EEF2FF"/>
                  <TextBlock x:Name="UserIdValue" Text="Discord ID: -" FontSize="14" Foreground="#93A4C9" Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                  <Button Content="Apply Selected Tweaks" Style="{StaticResource PrimaryButton}" Width="190" Height="36" Margin="0,0,10,0" Click="ApplySelectedTweaksButton_Click"/>
                  <Button Content="Copy ID" Background="#1B2D4D" Foreground="#DDE7FF" BorderThickness="0" Width="110" Height="36" Margin="0,0,10,0" Click="CopyIdButton_Click"/>
                  <Button Content="Log Out" Background="#4D1E2A" Foreground="#FFD7DF" BorderThickness="0" Width="110" Height="36" Click="LogoutButton_Click"/>
                </StackPanel>
              </DockPanel>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource FlatPanel}" Padding="18">
              <Grid>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Available Tweaks (loaded after authentication)" FontSize="22" FontWeight="Bold" Foreground="#EEF2FF"/>

                <ListBox x:Name="TweakListBox" Grid.Row="1" Margin="0,12,0,0" Background="#0D1528" BorderThickness="0" Foreground="#E2E8F0" SelectionMode="Extended" DisplayMemberPath="Name"/>
              </Grid>
            </Border>
          </Grid>
        </Grid>

        <Border Grid.Row="2" Margin="14,0,14,14" Background="#0A152C" CornerRadius="10" Padding="12,10">
          <TextBlock x:Name="StatusText" Foreground="#93C5FD" Text="Ready" TextWrapping="Wrap" />
        </Border>
      </Grid>
    </Border>
  </Grid>
</Window>
